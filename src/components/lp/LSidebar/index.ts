import LSidebarDesktop from "./components/LSidebarDesktop";
import LSidebarMobile from "./components/LSidebarMobile";

const LSidebar = {
  Mobile: LSidebarMobile,
  Desktop: LSidebarDesktop,
};

export default LSidebar;
